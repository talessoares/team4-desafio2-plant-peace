# Arquivo: nginx.conf
worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        
        location /images/ {
            root /usr/share/nginx/html;
            add_header Access-Control-Allow-Origin *;
        }
    }
}
